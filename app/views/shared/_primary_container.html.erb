<div class="w-full">
  <div class="flex justify-between items-center mb-6">
    <h1 class="font-bold text-4xl"><%= title %></h1>

    <%= render 'shared/action_link_primary', name: new_singular_lower(title), path: path %>
  </div>

  <%= yield %>
</div>